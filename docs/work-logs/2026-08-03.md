# 2026-08-03

## セッション1: marathon-coach を廃止として記録し、Vercel は破壊的操作の手前で止めた（TASK-20260802-036）

### 実装した機能
- なし（廃止タスク。アプリのコードは1行も変更していない）

### 修正したバグ
- なし

### ドキュメント / 記録
- **README.md**: 冒頭に「🛑 開発終了（2026-08-02）・後継は WtN」バナーを追加。
  併せて §5「データベースの初期化」に警告を1つ追加 —— この手順が指す本番DBには
  **別アプリ chirashi の現役データ約30MB** が入っており、`schema.sql` を流すと事故になるため。
  上から読まない人（手順だけ探しに来た人）に鳴らないバナーは無いのと同じ、という判断。
- **`brain-personal/projects/marathon-coach.md`**（SessionStart で毎回注入されるメモ）を
  禁止事項つきの廃止済みメモに書き換え: schema.sql を流さない / Gemini を差し替えない /
  Neon ストア `neon-emerald-island` に触らない / Vercel 削除は人間判断。
- **`brain-personal/decisions/marathon-coach/2026-08-03-TASK-036-廃止の記録と後片付けの範囲.md`** を作成。

### ビルド・配布
- **デプロイしていない。ビルドもしていない。** Vercel の状態は1バイトも変更していない
  （プロジェクト削除・Neon 切断・env 削除・auto-deploy 停止のいずれも 0回）。
- ブランチ `task/TASK-20260802-036` は**ローカルコミットのみで push していない**。
  このリポジトリは task ブランチを1本も push していない（`git branch -r` は `origin/main` のみ）＝既存の運用に合わせた。
  加えて、このリポジトリは GitHub 連携が生きており、push すると Vercel のビルドが走る。
  「畳むかどうか」を人間が判断する前に、廃止対象のプロジェクトへ新しいデプロイを積みたくなかった。

### 実測（read-only・③実データ依存の証拠）
- `vercel integration ls marathon-coach` → **`No resources found.`**
  `vercel integration ls chotto-good` → `neon-emerald-island … Projects: chotto-good`
  ＝ TASK-20260802-037 の保護は完了していた（**037 の報告を鵜呑みにせず自分で再測した**）。
- 本番3エンドポイントは 032 の実測から不変: `/` 200 / `/api/stats` 500 `42P01` / `/api/coach` 405。
- env（production）は `BLOB_READ_WRITE_TOKEN` / `GEMINI_API_KEY` の**2本のみ**。
- Blob store `marathon-images`（1ファイル・559.03KB・232日前）が**1つ生きている**。

### 備考（判断・教訓）
- **想定外1: 本番デプロイが「232日前で最後」ではなかった。** 最新は **2026-08-02 08:19 JST（1日前）**の
  production デプロイ。**GitHub `main` への push で自動デプロイが今も生きている**
  ＝「凍結＝今後 push しない」は人間の運用にしか依存しておらず、機械側のガードは無い。
- **想定外2: env を消しても本番は落ちていない。** 037 の disconnect で `DATABASE_URL` 等16本が
  消えた後も `/api/stats` は「DB未設定」ではなく `42P01` を返す ＝ **配信中のデプロイは当時の env
  スナップショットで今も Neon に到達できている**。「env を消した＝もう届かない」ではない。
  **再デプロイして初めて届かなくなる。**
- **想定外3: Blob store が1つ生きていた**（起票時のスコープにも 021/024/032 にも出てこない資産）。
  ただし本番DBに marathon-coach のテーブルが1つも無い＝参照する行が存在しない**孤児**。
- **越境の壁で `projects-index.md` が書けなかった**（生成物で source の `registry.json` の実体が
  mission-control 本体）。手編集は次の再生成で消えるので「書けたこと」にせず、
  `TASK-20260803-042`（mission-control・`--no-launch`）に起票＋パッチ全文をタスクファイルに用意した。
  同種の穴として mission-control に `TASK-20260803-032`（skills が書けない）が既に立っている。
- **Vercel の畳み方（削除 or 残置）は本人ゲート**なので、判断材料（消すと何を失うかの表・A/B/C）
  だけ出して止めた。036 の推奨は **A（プロジェクト削除）** —— 失う実データがゼロで、
  B/C が抱える「うっかり push で今より壊れた状態が公開される」口を根本から塞げるため。
- 作業中に `vercel link` が作った `.env.local` / `.vercel/` は削除し、`.gitignore` に足された
  `.env*` の1行も戻した。**秘密値はリポジトリにも vault にも1バイトも残していない。**
